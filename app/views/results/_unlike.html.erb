<%= form_with(url: favorite_path(qa_id :@qa.id), local: true),
             html: { method: :delete }) do |f| %>
             <div><%= hidden_field_tag :user_id, current_user.id %></div>

  <%= f.submit "UNLIKE", class: "btn" %>
<% end %>
