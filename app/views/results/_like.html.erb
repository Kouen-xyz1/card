<%= form_with(url: favorites_path, local: true) do |f| %>
<div><%= hidden_field_tag :user_id, current_user.id %></div>
  <div><%= hidden_field_tag :qa_id, @qa.id %></div>
  <%= f.submit "LIKE", class: "btn btn-primary" %>
<% end %>
