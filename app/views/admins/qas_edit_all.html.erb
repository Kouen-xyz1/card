<% @qas.each do |qa| %>

  <%= form_with(model: qa, local: true) do |f| %>
  <%= qa.id %>,
  <%= qa.episode_id %>
  <%= qa.episode.drama.title_japanese%>

    <%= f.label :question, '問題' %>
    <%= f.text_field :question %>

    <%= f.label :question, '解答' %>
    <%= f.text_field :answer %>

    <%= f.label :question, '音声ファイル' %>
    <%= f.text_field :soundfile %>

    <%= f.submit '更新する' %>
    <%#= link_to '削除する', qa, method: :delete, data: { confirm: '本当に削除してよろしいですか？' } %>

  <% end %>

<% end %>
